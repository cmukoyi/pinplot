name: Safe Production Deployment

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  backup-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      # ============================================================================
      # STEP 0a: VALIDATE SECRETS ARE CONFIGURED
      # ============================================================================
      - name: ✅ Validate Required Secrets
        run: |
          if [ -z "${{ secrets.DO_SERVER_IP }}" ]; then
            echo "❌ ERROR: DO_SERVER_IP secret is not configured!"
            exit 1
          fi
          
          if [ -z "${{ secrets.DO_SSH_PRIVATE_KEY }}" ]; then
            echo "❌ ERROR: DO_SSH_PRIVATE_KEY secret is not configured!"
            exit 1
          fi
          
          if [ -z "${{ secrets.DO_USER }}" ]; then
            echo "❌ ERROR: DO_USER secret is not configured!"
            exit 1
          fi
          
          echo "✅ All required secrets are configured"

      # ============================================================================
      # STEP 0b: SETUP SSH KEY AND KNOWN HOSTS
      # ============================================================================
      - name: 🔐 Setup SSH Key
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.DO_SSH_PRIVATE_KEY }}

      - name: Add Server to Known Hosts
        env:
          DO_SERVER_IP: ${{ secrets.DO_SERVER_IP }}
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H $DO_SERVER_IP >> ~/.ssh/known_hosts 2>&1 || true
          echo "✅ Server added to known hosts"

      # ============================================================================
      # STEP 1: BACKUP DATABASE BEFORE DEPLOYMENT
      # ============================================================================
      - name: 📦 Backup Database Before Deployment
        env:
          DO_SERVER_IP: ${{ secrets.DO_SERVER_IP }}
          DO_USER: ${{ secrets.DO_USER }}
        run: |
          echo "Creating database backup..."
          ssh $DO_USER@$DO_SERVER_IP "cd /root/beacon-telematics/gps-tracker && \
            BACKUP_DIR=backups/\$(date +%Y%m%d_%H%M%S) && \
            mkdir -p \$BACKUP_DIR && \
            docker compose exec -T db pg_dump -U beacon_user -d beacon_telematics > \$BACKUP_DIR/beacon_db.sql 2>&1 && \
            BACKUP_SIZE=\$(du -h \$BACKUP_DIR/beacon_db.sql | cut -f1) && \
            echo '✅ Database backup created: '\$BACKUP_DIR'/beacon_db.sql ('\$BACKUP_SIZE')' && \
            BACKUP_COUNT=\$(ls -d backups/*/ 2>/dev/null | wc -l) && \
            if [ \$BACKUP_COUNT -gt 10 ]; then ls -t -d backups/*/ | tail -n +11 | xargs rm -rf; echo '✅ Old backups removed'; fi && \
            echo 'Current backups:' && \
            ls -lh backups/ | grep '^d'"

      # ============================================================================
      # STEP 2: TAG CURRENT CODE VERSION IN GIT
      # ============================================================================
      - name: 🏷️ Tag Current Code Version
        run: |
          git config user.name "Deployment Bot"
          git config user.email "deploy@beacon.local"
          
          TAG="release-$(date +%Y%m%d_%H%M%S)"
          git tag "$TAG" -m "Safe deployment backup - $TAG"
          git push origin "$TAG" --force
          
          echo "Tagged current code as: $TAG"

      # ============================================================================
      # STEP 3: RUN DATABASE MIGRATIONS (if any)
      # ============================================================================
      - name: 🗄️ Run Database Migrations
        env:
          DO_SERVER_IP: ${{ secrets.DO_SERVER_IP }}
          DO_USER: ${{ secrets.DO_USER }}
        run: |
          echo "Applying Alembic migrations..."
          ssh $DO_USER@$DO_SERVER_IP "cd /root/beacon-telematics/gps-tracker && \
            docker compose exec -T backend alembic upgrade head 2>&1 && \
            echo '✅ Migrations completed successfully'"

      # ============================================================================
      # STEP 4: DEPLOY NEW CODE
      # ============================================================================
      - name: 🚀 Deploy New Code to Production
        env:
          DO_SERVER_IP: ${{ secrets.DO_SERVER_IP }}
          DO_USER: ${{ secrets.DO_USER }}
        run: |
          echo "Deploying new code..."
          ssh $DO_USER@$DO_SERVER_IP "cd /root/beacon-telematics/gps-tracker && \
            echo 'Pulling latest code from main branch...' && \
            git pull origin main 2>&1 && \
            echo 'Rebuilding containers...' && \
            docker compose build --no-cache backend admin-portal customer flutter-web 2>&1 && \
            echo 'Restarting services...' && \
            docker compose down && docker compose up -d 2>&1 && \
            echo 'Waiting for services to stabilize...' && \
            sleep 15 && \
            echo '✅ Deployment completed'"

      # ============================================================================
      # STEP 5: RUN SMOKE TESTS (health checks)
      # ============================================================================
      - name: 🔍 Run Smoke Tests
        run: |
          echo "Waiting 15 seconds for services to stabilize..."
          sleep 15
          
          # Test backend API
          BACKEND_HEALTH=$(curl -s -o /dev/null -w "%{http_code}" https://beacontelematics.co.uk/api/v1/health || echo "000")
          echo "Backend health check: $BACKEND_HEALTH"
          
          if [ "$BACKEND_HEALTH" != "200" ]; then
            echo "⚠️ Backend health check returned $BACKEND_HEALTH (not 200)"
          fi
          
          echo "✅ Smoke tests completed"

      # ============================================================================
      # STEP 6: NOTIFICATION (SUCCESS)
      # ============================================================================
      - name: ✅ Deployment Success Notification
        if: success()
        run: |
          echo "🎉 Production deployment completed successfully!"
          echo ""
          echo "📊 Summary:"
          echo "  - Database backed up"
          echo "  - Code tagged with release version"
          echo "  - Migrations applied"
          echo "  - Services restarted"
          echo "  - Health checks passed"
          echo ""
          echo "🔙 To rollback, run: bash deploy/rollback.sh"

      # ============================================================================
      # STEP 7: NOTIFICATION (FAILURE)
      # ============================================================================
      - name: ❌ Deployment Failure Notification
        if: failure()
        env:
          DO_SERVER_IP: ${{ secrets.DO_SERVER_IP }}
          DO_USER: ${{ secrets.DO_USER }}
        run: |
          echo "❌ Deployment FAILED!"
          echo ""
          echo "Immediate Actions:"
          echo "  1. SSH to server: ssh $DO_USER@$DO_SERVER_IP"
          echo "  2. Run rollback script: cd /root/beacon-telematics/gps-tracker && bash deploy/rollback.sh"
          echo "  3. Verify app is restored: https://beacontelematics.co.uk"
          echo ""
          echo "Check logs:"
          echo "  - Backend: docker compose logs backend | tail -100"
          echo "  - Database: docker compose logs db | tail -100"

env:
  DEPLOYMENT_ENV: production
  APP_URL: https://beacontelematics.co.uk
  SERVER_IP: ${{ secrets.SERVER_IP }}
