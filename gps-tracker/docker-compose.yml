

# Production Docker Compose — Pinplot
# Deployed via GitHub Actions CI/CD
# Secrets injected from backend/.env (written by pipeline from GitHub Secrets)

services:
  db:
    image: postgres:15-alpine
    container_name: pinplot_db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-pinplot_db_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-pinplot_db}
    volumes:
      - pinplot_postgres_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    networks:
      - pinplot_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-pinplot_db_user} -d ${POSTGRES_DB:-pinplot_db}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: pinplot_backend
    env_file:
      - ./backend/.env
    ports:
      - "0.0.0.0:8001:8000"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - pinplot_network
    restart: unless-stopped
    volumes:
      - nginx_logs:/var/log/nginx:ro
    environment:
      - DEBUG=False
      - ENVIRONMENT=production
      - FROM_EMAIL=noreply@pinplot.me
    # No --reload in production
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 2

  admin-portal:
    build:
      context: ./admin-portal
      dockerfile: Dockerfile
    container_name: pinplot_admin
    ports:
      - "3010:80"
    depends_on:
      - backend
    networks:
      - pinplot_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  customer:
    build:
      context: ./customer-dashboard
      dockerfile: Dockerfile
    container_name: pinplot_customer
    ports:
      - "3011:3001"
    environment:
      - BACKEND_URL=http://backend:8000/api/v1
      - NODE_ENV=production
    depends_on:
      - backend
    networks:
      - pinplot_network
    restart: unless-stopped

  flutter-web:
    build:
      context: ./mobile-app
      dockerfile: Dockerfile
    container_name: pinplot_flutter_web
    ports:
      - "3012:80"
    depends_on:
      - backend
    networks:
      - pinplot_network
    restart: unless-stopped

  mosquitto:
    image: eclipse-mosquitto:2.0
    container_name: pinplot_mosquitto
    volumes:
      - ./mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
      - mosquitto_data:/mosquitto/data
    networks:
      - pinplot_network
    restart: unless-stopped

  mqtt-bridge:
    build:
      context: ./mqtt-bridge
      dockerfile: Dockerfile
    container_name: pinplot_mqtt_bridge
    environment:
      # URL of the Pareto Anywhere Socket.IO server (container must be on this network)
      - PARETO_URL=http://pareto-anywhere:3001
      # Internal MQTT broker
      - MQTT_URL=mqtt://mosquitto:1883
      # Gateway positions: JSON map of receiverId (lowercase hex, no colons) → {x, y}
      # Example: '{"aabbccddeeff":{"x":100,"y":200},"112233445566":{"x":400,"y":350}}'
      - GATEWAY_POSITIONS={}
    depends_on:
      - mosquitto
    networks:
      - pinplot_network
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    container_name: pinplot_nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - nginx_logs:/var/log/nginx
    depends_on:
      - backend
      - admin-portal
      - customer
      - flutter-web
      - mosquitto
    networks:
      - pinplot_network
    restart: unless-stopped

volumes:
  pinplot_postgres_data:
    driver: local
  nginx_logs:
    driver: local
  mosquitto_data:
    driver: local

networks:
  pinplot_network:
    driver: bridge