# Mosquitto broker config — pinplot BLE tracking

# Standard MQTT (for internal bridge)
listener 1883
protocol mqtt

# WebSocket (proxied through nginx as wss://pinplot.me/mqtt)
listener 9001
protocol websockets

allow_anonymous true
